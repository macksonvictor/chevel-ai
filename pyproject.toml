[project]
name = "chevel-ai"
version = "0.1.0"
description = "Local CHEVEL AI MVP with Python and optional C++ helpers."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

